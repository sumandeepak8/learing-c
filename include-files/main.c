#include <stdio.h>
#include "sum.h"
#include "sub.h"

int main(void){
  printf("the value of sum of a and b is : %d\n", sum(2,3));
  printf("the value of sub of a and b is : %d\n", sub(3,1));
  return 0;
}
